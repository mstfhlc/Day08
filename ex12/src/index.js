// Only change code below this line
function randomInteger(x){
  

    if(x >0 || x <30){

    }
    return x=Math.floor(Math.random(x)*10);
  
}

// Only change code below this line
console.log(randomInteger(0,30));
module.exports=randomInteger;